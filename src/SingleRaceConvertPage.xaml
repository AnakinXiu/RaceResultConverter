<UserControl x:Class="RaceResultConverter.SingleRaceConvertPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceResultConverter"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:SingleRaceConvertViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="StackPanel">
                <Setter Property="Margin" Value="5" />
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="General.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="{x:Static local:Resource.lbSelectFile}"
                       Style="{StaticResource General.TextBlock}"/>
        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource General.TextBox}"
                     Text="{Binding SelectedFilePath}"
                     IsReadOnly="True"/>

        <Button Grid.Row="0" Grid.Column="2" 
                Content="{x:Static local:Resource.btnSelectFile}"
                Command="{Binding Path=SelectFileCommand }"
                Style="{StaticResource General.Button}"/>

        <Button Grid.Column="0" Grid.Row="3" 
                Content="{x:Static local:Resource.btnConvert}"
                Command="{Binding Path=ConvertCommand }"
                Style="{StaticResource General.Button}"/>
    </Grid>
</UserControl>